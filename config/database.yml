# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: sqlite3
  database: db/development.sqlite3
  pool: 5
  timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test: &test
  adapter: sqlite3
  database: db/test<%= ENV['TEST_ENV_NUMBER'] %>
  pool: 5
  timeout: 5000

# pool set to 25: that is the number of concurrent connections
#                 needs to be 25 because that's the max for sidekiq
production:
  adapter: postgresql
  database: NotistApp
  username: notistapp
  password: notablenotist
  host: notistapppgdb.ci7lbldq35nq.us-east-1.rds.amazonaws.com
  port: 5432
  pool: 25
  timeout: 5000

cucumber:
  <<: *test
