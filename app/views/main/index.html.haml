.wrapper
	%h2 Activities

	.feed-wrapper
		- @activities.each do |activity|
			.activity-item
				%div.circular#activity-image{:style => "background:url(#{activity.user.user_fb_data.profile_image}) no-repeat"}
				.act.tooltip.fade.right.in
					.tooltip-arrow
					.tooltip-inner
						.activity-type
							%div{:class => "#{activity.trackable.class.name.downcase}-type" }
								= activity.trackable.class.name.upcase
						= ActivityPresenter.new(activity, self).render_activity


/ <h1>Activities</h1>

/ <% @activities.each do |activity| %>
/   <%= div_for activity do %>
/     <%= link_to activity.user.name, activity.user %>
/     <%= render "activities/#{activity.trackable_type.underscore}/#{activity.action}", activity: activity %>
/   <% end %>
/ <% end %>
